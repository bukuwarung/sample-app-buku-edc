## 1. Implementation
- [x] 1.1 Add Balance Check navigation event from Home and route it to the Balance Check flow entry.
- [x] 1.2 Add Balance Check routes to the navigation graph and map them to existing Transfer screens with a Balance Check variant.
- [x] 1.3 Extend shared Transfer screens/view models to accept a `FlowVariant` and display Balance Check copy (titles, labels, CTA).
- [x] 1.4 Add Balance Check summary and receipt preview UI using existing composables extended for Balance Check data.
- [x] 1.5 Provide mock data sources for Balance Check summary/receipt fields in the UI layer.

## 2. Validation
- [x] 2.1 Manually verify Transfer flow remains unchanged.
- [x] 2.2 Manually walk through Balance Check flow to ensure screens match the Balance Check Figma and navigation is correct.